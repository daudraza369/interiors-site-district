# Run This Command

Open a terminal in the `district-interiors` folder and run:

```bash
npm run seed:contact-defaults
```

When it asks:
```
Accept warnings and push schema to database? » (y/N)
```

Type: `y` and press Enter

This will populate all Contact Section fields in the admin panel.





